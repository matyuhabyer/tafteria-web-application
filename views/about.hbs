<div class="w-full bg-yellow-50 min-h-screen flex flex-col items-center py-16 px-4">
    <h1 class="text-4xl font-bold mb-4 text-gray-800">About This Project</h1>
    <p class="text-lg mb-6 text-gray-700">This project utilizes several NPM packages and third-party libraries to provide its functionality. Here is a list of the dependencies and libraries used:</p>
    <div class="w-full max-w-4xl mx-auto bg-white p-8 rounded-lg shadow-lg">
        <div class="text-black">
            <h2 class="text-2xl font-semibold mb-4">NPM Packages</h2>
            <ul class="list-none mb-6">
                {{#each data.npmPackages}}
                <li class="mb-2">
                    <strong>{{name}}</strong> (Version: {{version}}) - {{description}}
                </li>
                {{/each}}
            </ul>
            <h2 class="text-2xl font-semibold mb-4">Third-Party Libraries</h2>
            <ul class="list-none">
                {{#each data.thirdPartyLibraries}}
                <li class="mb-2">
                    <strong>{{name}}</strong> - {{description}}
                </li>
                {{/each}}
            </ul>
        </div>
    </div>
</div>
